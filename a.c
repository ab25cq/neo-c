#include <neo-c.h>

int main(int argc, char** argv)
{
    int status;
    
                                          (((signed char) (((
                                          status
                                          ) & 0x7f) + 1) >> 1) > 0);
    
    return 0;
}
