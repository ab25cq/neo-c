#include <neo-c.h>

int main(int argc, char** argv) 
{
    int s[16];
    int{} p = span borrow s;
    
    p++;
    p++;
    printf("%d\n", p.position());
                                   
    return 0;
}
