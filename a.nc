#include <neo-c.h>
                            
int main(int argc, char** argv) 
{
    (FILE* f = fopen("UHHHH", "r")).elif {
        die("file not found");
    }
                                    
    return 0;
}
