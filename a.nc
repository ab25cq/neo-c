#include <neo-c.h>

void fun(int x=2, int y=1)
{
    printf("x %d y %d\n", x, y);
}
                            
int main(int argc, char** argv) 
{
    fun(x:3);
                                    
    return 0;
}
