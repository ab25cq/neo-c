#include <neo-c.h>

void fun(int x, int y=1)
{
    printf("x %d y %d\n", x, y);
}
                            
int main(int argc, char** argv) 
{
    fun(z:3);
                                    
    return 0;
}
