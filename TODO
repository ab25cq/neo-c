const char*をまともにする。

init_main のinherit

配列の初期化

machine stack automatically free
error number bug in if, impl
----------------------
uniq extern callocとfreeの数を数えてメモリーリークの有無を調べる。

debug info

function call list, memory leakの場所を特定する。-gオプション

, 演算子
__extention__ (exp)

sizeof(exp)

if() ; else ;

if(true) {
}
*v = が}のあとに;をつけないとだめ。パーサーのバグ
int a = true; => a == -1
-g option
LLVM3.8 OPTIMIZE
LLVM7.0 -g
-g ivalid line number
char** argv

argv[0][0]


static local variable
alloc initialize
alloc destructor
rm neo-c.h.out
Array casts Pointer
if error line number bug

neo-c.hのインライン化
glibcへの非依存
Pointer minus Pointer valid size
if(a) printf("AAA");
ポインタとポインタの減算をC互換にする。 wiのポインタとポインタの減算を修正。
