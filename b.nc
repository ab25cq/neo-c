#include <neo-c.h>

int main(int argc, char** argv)
{
    if(1) {
        string a = if(1) {
            puts("AAA");
            s"ABC"
        }
        
        puts(a);
    }
    
    return 0;
}
