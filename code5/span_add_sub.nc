#include <neo-c.h>

int main(int argc, char** argv)
{
    int x[4] = { 10, 20, 30, 40 };
    
puts("AAA");
    int{} s = span x;

    printf("%d\n", *s);
    s++;
    printf("%d\n", *s);

    return 0;
}
